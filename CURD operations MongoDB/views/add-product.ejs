<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <form action="<%=edit ? '/edit'  : '/add' %>" method="post">
      <input
        type="text"
        name="title"
        value="<%=edit ? data[0].title  : '' %>"
      />
      <input
        type="number"
        name="price"
        value="<%=edit ? data[0].price  : '' %>"
      />
      <% if(edit){%>
      <input type="hidden" value="<%=data[0]._id %>" name="id" />
      <% }%>
      <button>send</button>
    </form>
  </body>
</html>
